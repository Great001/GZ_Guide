<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/iv_location_icon"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_marginLeft="10dp"
        android:padding="5dp"
        android:src="@drawable/location_black"
        android:layout_centerVertical="true"/>

    <TextView
        android:id="@+id/tv_addr_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/iv_location_icon"
        android:layout_marginLeft="15dp"
        android:text="华师"
        android:textColor="@color/lv_item_title"
        android:textSize="@dimen/lv_item_title_size"/>

    <TextView
        android:id="@+id/tv_addr_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_addr_name"
        android:layout_alignLeft="@id/tv_addr_name"
        android:layout_marginTop="10dp"
        android:singleLine="true"
        android:ellipsize="end"
        />

</RelativeLayout>